/**
 * Enquanto não há autenticação, todo XP/conquista é atribuído ao perfil padrão
 * (seed `id = 1`). Centralizado aqui para trocar fácil quando houver login.
 */
export const DEFAULT_PROFILE_ID = 1;
